# Dependencies
node_modules/

# Env / secrets — never commit these
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Per-video working artifacts (large media files)
videos/
*.mp4
*.mov
*.mp3
*.wav
*.m4a
assets/outro-spoken.mp3

# Test-generated fixtures (media + scratch). Intentional fixtures are force-added (git add -f).
tests/fixtures/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
